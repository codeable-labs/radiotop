[build]
nixpacksVersion = "1.20.0"

[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "nodejs"]

[phases.build]
cmds = [
  "composer install",
  "npm install"
  "npm run build"
]

[start]
cmd = "php -S 0.0.0.0:3000 -t public"